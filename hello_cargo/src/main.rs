fn main() {
    println!("Hi, Mom!");
}

/*
 * Cargo Commands:
 * 
 * 1) To compile the code to exe -> 'cargo build'
 * 2) To compile to exe AND run the exe -> 'cargo run'
 * 3) To check if the code compiles WITHOUT building an exe -> 'cargo check'
 * 4) To build a release version of the code -> 'cargo build/run --release'
 * 5) To create a new project -> 'cargo new <project_name>'
 */